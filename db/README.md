# CoffeeCApp - Database Main (MySQL)

## Nome DB
coffe_app

## Setup rapido (PC fisso con MySQL/Workbench)
1) Crea DB `coffe_app`
2) Esegui `01_schema_main.sql`
3) Verifica:
    - SHOW TABLES;
    - SELECT * FROM users;
    - SELECT * FROM distributors;
    - SELECT * FROM beverages;

## Credenziali suggerite
user: coffe
password: coffe_pwd